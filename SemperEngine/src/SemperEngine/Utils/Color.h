#include <glm/glm.hpp>
#include "SemperEngine/Core/Defines.h"

namespace SemperEngine
{
	namespace Util
	{
		// class Color
		// {
		// public:
		// 	float r, g, b, a;
		// 	float h, s, v;
		// 
		// 	Color(const glm::vec4 &colorRGBA) SE_NOEXCEPT;
		// 	~Color() SE_NOEXCEPT;
		// 
		// 	SE_FORCE_INLINE glm::vec4 GetRGBA() SE_CONST SE_NOEXCEPT { return glm::vec4 { r, g, b, a }; }
		// 	SE_FORCE_INLINE glm::vec3 GetHSV() SE_CONST SE_NOEXCEPT { return glm::vec3 { h, s, v }; }
		// 
		// 	static glm::vec4 &RGBA(float r, float g, float b, float a) SE_NOEXCEPT;
		// 	static glm::vec4 &RGBA255(float r, float g, float b, float a) SE_NOEXCEPT;
		// 
		// 	static glm::vec4 &HSV(float r, float g, float b, float a) SE_NOEXCEPT;
		// };
	}
}